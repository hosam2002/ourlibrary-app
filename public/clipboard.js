/*
const ourlibrary = `https://ourLibrarySite.onrender.com`

const blogShareButton = document.getElementById(`share-link`)

blogShareButton.addEventListener('click', (e) => {
    
    e.preventDefault()

    const data = blogShareButton.dataset.share
    const link = data.replace('%domain%', ourlibrary)

    navigator.clipboard.writeText(link)
    alert(`Link copied`)
})
*/
